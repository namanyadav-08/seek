# Render Blueprint — https://render.com/docs/blueprint-spec
services:
  - type: web
    name: seek
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: GOOGLE_API_KEY
        sync: false
